<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ezen.team.mapper.BoardMapper">

	<!-- 1:1문의 등록 -->
	<insert id="csInsert" parameterType="csDTO">
		INSERT INTO cs_master(user_id, cs_code, cs_dt, cs_title, cs_con)
		VALUES(#{user_id},#{cs_code}, now(), #{cs_title},#{cs_con}) 
	</insert>
	
	
	<!-- Notice 가져오기 -->
	<select id="getNoticeList" resultType="hashmap">
	select * from post_mng where pst_gb = '002'
	</select>
	
	<!-- 해당번호에 맞는 공지 글 가져오기 -->
	<select id="getNoticeInfo" resultType="BoardDTO">
	select * from post_mng where pst_no = #{no}
	</select>
</mapper>